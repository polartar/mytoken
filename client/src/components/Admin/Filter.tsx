import React from 'react';

const Filter = () => {
  // TODO: Implement a fiilter input with optional + button
  // It must be generic to be used on the three screens
  return <div>Filter</div>;
};

export default Filter;
